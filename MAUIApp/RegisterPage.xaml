<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUIApp.RegisterPage"
             BackgroundColor="#f0f2f5">
    <ScrollView>
        <VerticalStackLayout Spacing="30" Padding="40,80,40,40" VerticalOptions="Start">
            
            <Frame BackgroundColor="White" CornerRadius="20" Padding="40" HasShadow="True">
                <VerticalStackLayout Spacing="25">
                    
                    <Label Text="📝" FontSize="60" HorizontalOptions="Center" />
                    <Label Text="Create Account" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#333" />
                    <Label Text="Please fill in the details" FontSize="16" HorizontalOptions="Center" TextColor="#666" />
                    
                    <Frame BackgroundColor="#f8f9fa" CornerRadius="10" Padding="0" HasShadow="False">
                        <Entry x:Name="NameEntry" Placeholder="👤 Full Name" BackgroundColor="Transparent" />
                    </Frame>
                    <Label x:Name="NameError" TextColor="Red" IsVisible="False" FontSize="12" />
                    
                    <Frame BackgroundColor="#f8f9fa" CornerRadius="10" Padding="0" HasShadow="False">
                        <Entry x:Name="EmailEntry" Placeholder="📧 Email" Keyboard="Email" BackgroundColor="Transparent" />
                    </Frame>
                    <Label x:Name="EmailError" TextColor="Red" IsVisible="False" FontSize="12" />
                    
                    <Frame BackgroundColor="#f8f9fa" CornerRadius="10" Padding="0" HasShadow="False">
                        <Entry x:Name="PasswordEntry" Placeholder="🔒 Password" IsPassword="True" BackgroundColor="Transparent" />
                    </Frame>
                    <Label x:Name="PasswordError" TextColor="Red" IsVisible="False" FontSize="12" />
                    
                    <Button x:Name="RegisterBtn" Text="Register" Clicked="OnRegisterClicked" 
                            BackgroundColor="#28a745" TextColor="White" 
                            CornerRadius="10" HeightRequest="50" FontSize="16" FontAttributes="Bold" />
                    
                    <Label Text="Already have an account? Sign In" HorizontalOptions="Center" TextColor="#007bff" FontSize="14">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnLoginClicked" />
                        </Label.GestureRecognizers>
                    </Label>
                    
                </VerticalStackLayout>
            </Frame>
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>